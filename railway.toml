[deploy]
startCommand = "python3 app.py"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
sleepApplication = true    ← ESTO ACTIVA EL SLEEP

[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile"